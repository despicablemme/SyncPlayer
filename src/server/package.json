{
  "name": "syncplay-server",
  "version": "2.0.0",
  "description": "SyncPlay - PeerJS 私有信令服务器（替换 PeerJS 公共服务器）",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "peer": "^0.6.1"
  },
  "engines": {
    "node": ">=16"
  }
}
