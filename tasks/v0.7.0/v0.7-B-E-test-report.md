# v0.7-B-E 测试报告 — 9 格式测试矩阵 + 同步回归 + 硬解证据链

> **commit**: <pending — Step 7>
> **日期**: 2026-07-25
> **决策依据**: `tasks/v0.7.0/02-execution-plan.md` §7 + §8 + §9
> **下一阶段**: B-F (升 version 0.6.2 → 0.7.0 + workflow_dispatch debug build)

---

## §0 Audit 结果 (per MEMORY #47 — 必先 audit)

| # | 检查 | 结果 |
|---|------|------|
| 1 | 现有所有 test 文件 | **6 文件** (3 单元 + 3 集成): `desktop/test/{unit,integration}/` 含 `container-transmux`, `hls-player`, `mse-player` 单元 + `hls-players`, `mse-passengers`, `transmux-passengers` 集成. 另外 root `test/unit/` 有 4 文件 (`sync-engine`, `video-history`, `video-match`, `room-state`) npm test 跑这些. |
| 2 | 主人 test sample 路径 | `/Volumes/Claw/太空旅客.BD.720p.中英双字幕.mkv` ✓ **1.5G (ls 显示) / 1.64 GB (按 1GB=1024MB 计)** — 跟 B-B 用的是同一个, 复现 |
| 3 | ffprobe 可用性 | `/opt/homebrew/bin/ffprobe` ✓ `ffprobe version 8.1.1` (B-B transmux 阶段已装) |
| 4 | Electron CDP / node | `node v26.5.0` ✓ (Electron 38 + electron-builder 25 配套) |
| 5 | SyncEngine / RoomStateMachine / VideoMatch 单元覆盖 | root `test/unit/{sync-engine,room-state,video-match,video-history}.test.js` 已存在, **112 测试 pass**. B-E 新增 `desktop/test/unit/sync-engine.test.js` 补充 17 测试 (MSE/hls.js/原生路径兼容 + drift_response 边界) |
| 6 | package.json scripts | `"test": "node --test ../test/unit/*.test.js"` (root 级), `"dist:mac": "electron-builder --mac"` ✓ |
| 7 | GitHub Actions workflow | `.github/workflows/build.yml` 存在, `workflow_dispatch` 接受 `version` + `build_type=debug|release` 输入 ✓ |
| 8 | desktop/public/ 当前 | `hls.min.js` + `ffmpeg/{ffmpeg-core.js, ffmpeg-core.wasm}` ✓ (B-A prebuild 拷过) |

---

## §1 测试 fixture

- **主人样本**: `/Volumes/Claw/太空旅客.BD.720p.中英双字幕.mkv` (1.5 GB on disk, H.264 + AAC 5.1) ✓
- **公网样本 URLs**: `desktop/test/fixtures/sample-urls.md` (7 个格式 URL + 1 个 mkv_h265 占位)
- **目录**: `desktop/test/fixtures/` 新建 (CLAUDE B-E 创建)

---

## §2 集成测试代码 (默认 skip, Electron renderer 跑)

| 文件 | 行数 | 测试数 | 默认 |
|------|------|--------|------|
| `desktop/test/integration/multi-format-matrix.test.js` | ~250 | 8 (mp4_h264 + mp4_h265 + webm_vp9 + webm_av1 + m3u8_hls + mkv_passengers + mkv_h265 + avi + flv) | **SKIP** (需 Electron renderer + SAB + DOM + MediaSource) |
| `desktop/test/integration/sync-dual-window.test.js` | ~200 | 3 (双窗口 mp4 + 双窗口 mkv + MSE 路径事件) | **SKIP** (需 Electron renderer 双窗口 + PeerJS DataChannel mock) |
| `desktop/test/integration/hw-decode-evidence.test.js` | ~140 | 4 (chrome://gpu + VTDecoderXPCService + 主进程 CPU + 端到端 HEVC) | **SKIP** (需主人 macOS 实测 + Activity Monitor) |

跳过机制: `process.env.SYNCPLAY_RUN_* === '1'` + sandbox 检测 (`typeof SharedArrayBuffer === 'undefined'` 等).

---

## §3 单元测试 (npm test + desktop test/unit)

| 范围 | 测试数 | pass | fail |
|------|--------|------|------|
| `npm test` (root `test/unit/*.test.js`) | **112** | 112 | 0 |
| `desktop/test/unit/*.test.js` (B-A 加 + B-E 加) | **51** (含 B-E 新增 17 sync-engine) | 51 | 0 |
| **合计** | **163** | 163 | 0 |

### B-E 新增 (desktop/test/unit/sync-engine.test.js)

| 测试组 | 数量 |
|--------|------|
| SyncEngine — bindVideoEvents (play/pause/seeked 触发 send, guardUntil 屏蔽窗口) | 2 |
| SyncEngine — handle() (play/pause/seek/heartbeat/drift_check/drift_response/file_info/空消息) | 13 |
| SyncEngine — start / stop (heartbeat + drift timers) | 2 |
| SyncEngine — MSE / hls.js / 原生路径兼容 | 2 |
| **小计** | **17** |

### 现有覆盖 (root test/unit, B-A 之前已有)

- sync-engine.test.js (root): 36 测试, 覆盖 handle/play/pause/seek/heartbeat/drift/file_info
- room-state.test.js (root): 26 测试, 覆盖合法/非法转移 + listener + canSync gating
- video-match.test.js (root): 14 测试, 覆盖 URL/文件名/时长匹配
- video-history.test.js (root): ~36 测试, 覆盖历史记录

---

## §4 9 格式测试矩阵 — 期望行为 (per Claude Round 2 §7)

| 格式 | 测试文件 | 期望路径 | 硬解期望 | 主人实测 |
|------|---------|---------|---------|---------|
| mp4 H.264 | BigBuckBunny.mp4 | 原生 `<video>` | Mac 软/硬解 | **[pending]** |
| mp4 H.265 | Big_Buck_Bunny_720_10s_1MB.mp4 | 原生 `<video>` | **VTDecoderXPCService CPU > 0** (硬解证据) | **[pending]** |
| webm VP9 | ForBiggerBlazes.webm | 原生 `<video>` | 软/硬解 (依平台) | **[pending]** |
| webm AV1 | ForBiggerJoyrides-AV1-8bit-51.webm | 原生 `<video>` | **AV1 硬解** (M1+ / RTX 30+ / RX 6000+) | **[pending]** |
| m3u8 HLS | x36xhzz.m3u8 | hls.js → MSE → `<video>` | hls.js 内置硬解 (hls.js → MSE pipeline) | **[pending]** |
| **mkv H.264** | **太空旅客.mkv 1.5 GB** | ffmpeg.wasm → fMP4 → MSE | **VTDecoderXPCService CPU > 0** (avc1 + MSE pipeline) | **[pending]** |
| mkv H.265 | 主人自己找 4K HEVC mkv | ffmpeg.wasm → fMP4 → MSE | **VTDecoderXPCService CPU > 0** (hvc1 + MSE pipeline) | **[pending]** |
| avi | Big_Buck_Bunny_360_10s_1MB.avi | ffmpeg.wasm → fMP4 → MSE | 软解 (codec 简单) | **[pending]** |
| flv | Big_Buck_Bunny_360_10s_1MB.flv | ffmpeg.wasm → fMP4 → MSE | 软解 | **[pending]** |

**决策树** (per `desktop/src/client/app.js` loadVideo):

```
url *.m3u8   → HlsPlayer → hls.js → MSE → <video>
url *.mkv/avi/flv/mov/wmv → transmuxToFmp4 (ffmpeg.wasm) → fMP4 → MsePlayer → MSE → <video>
url *.mp4 / *.webm / blob → video.src = src (Chrome 原生 + 硬解)
```

---

## §5 同步回归期望 (per Claude Round 2 §7)

| 场景 | 期望 | 主人实测 |
|------|------|---------|
| 双窗口 mp4 同步 | play/pause/seek 双方 UI 实时同步 (< 500ms) | **[pending]** |
| 双窗口 mkv (太空旅客.mkv) 同步 | 同上, ffmpeg.wasm → fMP4 → MSE 路径 | **[pending]** |
| MSE 路径下 video 事件触发 SyncEngine | play/pause/seeked/currentTime 正常 (单元测试已覆盖, 集成测试 skip) | **单元测试 PASS** |
| 双方都加载后状态 | RoomStateMachine IN_ROOM_SYNCED, dot=connected 绿 | **[pending]** |
| 一方进/退房另一方反应 | 状态正确恢复 (root room-state.test.js 已覆盖转移逻辑) | **单元测试 PASS** |

---

## §6 硬解证据链 (per Claude Round 2 §8 — 3 项并列)

| 证据 | 期望 | 主人实测 |
|------|------|---------|
| **chrome://gpu**: "Video Acceleration Information" 段有 "Decode hevc main" / "Decode av1 main" / etc. | ✓ | **[pending]** |
| **Mac Activity Monitor**: `VTDecoderXPCService` 进程 CPU > 0 当 HEVC 视频播放 | ✓ | **[pending]** |
| **主进程 CPU**: SyncPlay 主进程 < 20% 当 HEVC 视频播放 (硬解 = GPU 工作) | ✓ | **[pending]** |

**主人实测命令** (阶段 C, B-F release 后):

```bash
# 1. 启动 SyncPlay dev build
cd ~/CodeProjects/syncplay/desktop && npm run dev

# 2. 加载 mp4 H.265 样本 + play
# 3. DevTools console: window.open('chrome://gpu')
#    → 看 "Video Acceleration Information" 段 → "Decode hevc main" ✓
# 4. macOS Activity Monitor 搜 "VTDecoderXPCService" → CPU% > 0 ✓
# 5. macOS Activity Monitor 搜 "Electron" (主进程) → CPU% < 20% ✓
```

---

## §7 DoD 验收 (per Claude Round 2 §9)

- [x] npm test 100% pass (112/112 root, 51/51 desktop = **163/163 总 pass**)
- [x] 集成测试代码完整 (`multi-format-matrix` + `sync-dual-window` + `hw-decode-evidence` 默认 skip)
- [x] 硬解证据链 3 项验证脚本就位 (实际跑阶段 C 主人)
- [x] 9 格式测试矩阵 (含 mkv h264/h265 + avi + flv) 占位写完
- [x] 同步回归测试占位写完
- [x] 测试报告 v0.7-B-E-test-report.md (本文档)
- [ ] 1 个 commit (本地, 不 push) — Step 7
- [ ] working tree 干净 — Step 7

---

## §8 阶段 B-F 准备

- ✅ 测试矩阵 + 同步回归 + 硬解证据链 全部就位
- ✅ 主人实测在 阶段 C (debug build trigger → 装 .dmg → 实测 7+ 格式)
- ✅ B-F (release 准备) 可以开始: 升 version 0.6.2 → 0.7.0 + workflow_dispatch debug build
- ✅ 5 trade-off 拍板 (per tasks/v0.7.0/01-fix-plan.md): 原生 + hls.js + ffmpeg.wasm 三轨; fMP4 默认 avc1; SyncEngine 不动; 测试矩阵 Electron renderer 跑; 硬解用证据链

---

## §9 下一步建议

- ✅ OK 进 v0.7-B-F (升 version + workflow_dispatch debug build)
- 主人实测阶段 C 时, 把 §4 / §5 / §6 表的 "[pending]" 替换为实测结果
- 主人实测命令见 `desktop/test/fixtures/sample-urls.md` 末尾

---

## 引用

- `tasks/v0.7.0/01-fix-plan.md` (5 trade-off + 6 子任务 commit plan)
- `tasks/v0.7.0/02-execution-plan.md` §7 + §8 + §9 (测试矩阵 / 硬解证据链 / DoD)
- `desktop/test/fixtures/sample-urls.md` (7 公网 URL + 主人本地)
- `desktop/test/integration/multi-format-matrix.test.js`
- `desktop/test/integration/sync-dual-window.test.js`
- `desktop/test/integration/hw-decode-evidence.test.js`
- `desktop/test/unit/sync-engine.test.js` (B-E 新增, 17 测试)
- `desktop/src/client/app.js` loadVideo() 决策树 (B-A/B/B/C/D 累积)
- `src/shared/sync-engine.js` (B-A 加)
- root `test/unit/{sync-engine,room-state,video-match,video-history}.test.js` (既有 112 测试)